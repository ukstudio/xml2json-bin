require "spec_helper"

describe Xml2json::Bin do
  it "has a version number" do
    expect(Xml2json::Bin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
